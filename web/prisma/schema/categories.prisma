model Category {
    id String @id @default(cuid())

    name        String
    icon        String

    includedInCategories ListCategory[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@index([name])
    @@index([includedInCategories])
    @@unique([name])
}
