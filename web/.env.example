# Connessione diretta al Postgres (usata da prisma.config.ts per `prisma db push`, `prisma studio`, ecc.)
DATABASE_URL=""

# Connection string di Prisma Accelerate (usata a runtime da PrismaClient per il pooling), da generare dopo aver collegato il DB
# PRISMA_ACCELERATE_URL=""
